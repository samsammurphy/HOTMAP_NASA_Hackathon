var volcanoes = ee.FeatureCollection('ft:1CFywBl54IDbjL-xuNrdVwexgQqZH-AP7IAM-l_L1')

//Map.addLayer(volcanoes)

var v = volcanoes.first()

//var point = ee.Geometry.Point(v.get('geometry'))


//var point = ee.Geometry.Point(v.get('geometry'))

var point = v.get('geometry').float()
print(point)